You can put here the flash OCX you want to execute:
It will detect two OCX here: Flash11a.ocx, Flash10u.ocx
If not can detect a OCX file here, it will use the system's one or will fail if flash is not installed.
You can grab them from (on x64 systems): C:\Windows\SysWOW64\Macromed\Flash
